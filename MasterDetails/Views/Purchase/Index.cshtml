@model IEnumerable<MasterDetails.Models.PurchaseInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PONO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProdctId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PONO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProdctId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrcuaseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrcuaseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrcuaseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
